# Acceptance

Reviewed at: 2026-07-25T00:24:52.900Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1 — Capability confines to worktree root**: When args carries a well-formed ADV capability `Symbol.for("advance.morph-worktree-capability.v1") = { root: <absolute canonical string>, … }`, morph confines `target_filepath` to `capability.root`. A path inside the worktree succeeds; a path outside the worktree is rejected. Verified with the REAL `resolveTargetPath`: an in-root path resolves; an out-of-root path is rejected. | pass | index.test.ts:1948 AC1 test uses real resolveTargetPath + mkdtempSync worktree; in-root target_filepath succeeds ('Created new file'), out-of-root /etc/passwd rejected ('outside allowed root'). Reviewer PASS. |
| AC2 | acceptance_criterion | **AC2 — No capability = unchanged behavior**: When no capability symbol is present, root is `context.worktree ?? context.directory ?? directory` (unchanged). All existing `resolveTargetPath` and `executeMorphEdit` tests pass unmodified. | pass | index.test.ts:1970 AC2: no capability symbol -> resolveTargetPath receives session directory '/project'; existing tests unmodified. |
| AC3 | acceptance_criterion | **AC3 — Malformed capability fails closed**: When the symbol is present but the value is malformed (not an object, or `root` missing / non-string / non-absolute), morph ignores it and uses the session root. No root override occurs → no confinement expansion. | pass | index.test.ts:1992 + :1925: malformed capabilities (relative root, non-object, throwing accessor) fall back to session root, no expansion. readCapabilityRoot fail-closed try/catch (src/adv-capability.ts:16). |
| AC4 | acceptance_criterion | **AC4 — Schema exposes workdir/taskId**: `morph_edit` zod args declare optional `workdir: string?` and `taskId: string?` (model-visible). Existing calls omitting them still pass schema validation. | pass | index.test.ts:2034: plugin loads with workdir+taskId args present; tool.schema.string().optional() validated by bun run typecheck. impl.ts:296. |
| AC5 | acceptance_criterion | **AC5 — Per-call confinement (concurrency-safe)**: Two concurrent `morph_edit` calls with different capability roots are independently confined to their respective roots. The capability read is per-call with no shared mutable state. | pass | index.test.ts:2090: Promise.all concurrent calls with /wt-a and /wt-b -> recording resolveTargetPath captures each root independently. Per-call, no shared state. |
| AC6 | acceptance_criterion | **AC6 — Runtime integration guard (two-tier)**: (Tier 1) A deterministic characterization test proves a non-enumerable `Symbol.for(...)` property attached via `Object.defineProperty(..., {enumerable:false, configurable:false, writable:false})` survives reference passing into `execute` and does NOT survive a shallow clone/spread/`Object.assign` — the JS-level signature of the dispatch invariant morph depends on. (Tier 2) A documented upgrade-verification checklist describes manual re-verification against the installed `opencode` binary (two-plugin before-hook→execute harness) on OpenCode upgrades. Rationale: OpenCode's dispatcher is a compiled binary (not importable) and its tool-invocation entry points are AI-mediated, so a deterministic automated e2e against the installed binary is not cleanly achievable. | pass | index.test.ts:2102 Tier1: symbol survives real executeMorphEdit reference path (capturedRoots==['/x']); dropped by {...args} and Object.assign({},args). Tier2 upgrade checklist documented (test.skip :2108). |
| AC7 | acceptance_criterion | **AC7 — No regression**: Full existing suite passes. | pass | Full bun run check green: 152 pass, 1 skip, 0 fail; typecheck+lint+format pass (adv_run_test tr_mrzm6x47_0f2ed1f1). No regressions. |
| C1 | constraint | **No ADV import**: morph cannot import from the `advance` package (independently versioned plugins). It redeclares the symbol key string `Symbol.for("advance.morph-worktree-capability.v1")` — `Symbol.for` is global and returns the same symbol by key. | respected | No 'advance' import anywhere (git grep confirmed empty). src/adv-capability.ts:4 redeclares Symbol.for('advance.morph-worktree-capability.v1'). |
| C2 | constraint | **Reuse confinement**: `resolveTargetPath` is reused unchanged; only the `root` argument differs. No second confinement mechanism, no weakened symlink/containment checks. | respected | src/path-confinement.ts untouched in diff; resolveTargetPath reused, only root arg differs (src/execute.ts:113). |
| C3 | constraint | **Custom-tool dispatch path only**: The symbol-survival guarantee is specific to OpenCode's custom-tool dispatch path (host plugins). MCP/code-mode paths are out of scope (different args flow). | respected | morph_edit is a host custom-tool plugin; no MCP/code-mode dispatch handling added. Symbol-survival scoped to custom-tool path. |
| C4 | constraint | **No new runtime dependency.** | respected | package.json dependencies unchanged; only node:path (stdlib) used in adv-capability.ts. |
| C5 | constraint | **Trust boundary**: morph trusts the attached symbol's `root` only after shape-validation, and still confines via `resolveTargetPath`. morph's own `path.isAbsolute`/`path.resolve` checks are lexical; confinement correctness relies on ADV attaching an already-canonicalized root (`authorizeMorphWorktree` canonicalizes before attaching). Model-supplied `workdir`/`taskId` are NOT trusted by morph for confinement (ADV validates them; morph never reads their values for root derivation). | respected | execute.ts/adv-capability.ts never read workdir/taskId for root derivation; morph trusts only the validated symbol root (readCapabilityRoot). Lexical validation relies on ADV canonicalization (documented). |
| DONT1 | avoidance | Do NOT weaken existing path confinement (symlink resolution, canonical containment). The capability only swaps which `root` is handed to the same `resolveTargetPath`. | respected | resolveTargetPath symlink/containment defenses unchanged; capability only swaps which root is passed. No weakened checks. |
| DONT2 | avoidance | Do NOT make morph depend on ADV at import/runtime (no cross-package dependency). | respected | No cross-package import; symbol key redeclared as global string. |
| DONT3 | avoidance | Do NOT trust model-supplied `workdir`/`taskId` values inside morph — they exist only so ADV can read+validate them. | respected | morph ignores model workdir/taskId values; they exist only for ADV before-hook validation. |
| DONT4 | avoidance | Do NOT expand scope to MCP/code-mode dispatch paths or to hostile-plugin resistance (global symbol is not secret; documented trust boundary = ADV/plugin-runtime). | respected | No MCP/code-mode scope or hostile-plugin resistance added; trust boundary documented. |
| DONT5 | avoidance | Do NOT add the capability read as a heuristic/optional security control — it is structural confinement with deterministic fail-closed semantics. | respected | Capability read is structural confinement with deterministic fail-closed (shape-validation + try/catch), not a heuristic control. |

