# Archive Briefing Digest

**Change ID:** improveMorphRecovery
**Title:** Improve Morph recovery
**Status:** archived
**Generated:** 2026-07-10T21:42:26.772Z

## Identity Anchors

- CHANGE
- STATUS
- TERMINAL_GATE_SUMMARY
- Origin: discovery

## Archive Digest

**Status:** archived

| Gate | Status |
| --- | --- |
| proposal | done |
| discovery | done |
| design | done |
| planning | done |
| execution | done |
| acceptance | done |
| release | pending |

## Epic Context

No Epic membership

## Durable Facts

Showing 71 of 71 durable facts.

- **[archive_only_evidence]** decisions: Used identical canonical recovery wording across all three surfaces (index.ts uses backtick-free variant because its description is a JS template literal; md files use backticked `edit`/`morph_edit`). — The design requires the same sequence on every owned surface; identical wording makes drift tests deterministic and lets one shared anchor set guard all three surfaces (SC4/DONT4) instead of duplicating a single surface.
- **[archive_only_evidence]** decisions: Added the recovery block as a named 'Native edit recovery' subsection (md files) and a 'NATIVE EDIT RECOVERY' block (index.ts), placed immediately before the existing Morph FALLBACK line. — Keeps the new native-edit-failure recovery structurally separate from the preserved Morph API-error/timeout fallback (SC3/AC4) and makes the 'fallback below' reference accurate.
- **[archive_only_evidence]** decisions: Used native edit (exact string replacements) for the four localized insertions rather than morph_edit. — Each change was a small, uniquely-anchored insertion — the native-edit use case; no API dependency needed and lower risk for whitespace-sensitive template-literal/markdown edits.
- **[archive_only_evidence]** decisions: Added an 'Unreleased' README changelog note instead of a version bump. — Documents the policy alignment per repo convention without implying a release/version change (C4: no release/publish/deploy).
- **[archive_only_evidence]** verification: bun /tmp/opencode/morph_recovery_red_check.mjs <worktree> (1) — RED (expected): proved strengthened anchors are absent from pre-change HEAD of index.ts, instructions/morph-tools.md, README.md -> the new regression test would have failed before doc alignment.
- **[archive_only_evidence]** verification: bun test -t "native edit recovery policy" (0) — GREEN: 3 recovery-policy tests pass, 33 expect() calls; all three surfaces contain the canonical anchors plus preserved API-fallback and write-boundary signals.
- **[archive_only_evidence]** verification: bun run ci (0) — VERIFY: full suite 133 pass / 0 fail (280 expect() calls) and tsc --noEmit clean (ci = bun test && tsc --noEmit); SC5 satisfied.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun /tmp/opencode/morph_recovery_red_check.mjs <worktree>
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun test -t "native edit recovery policy"
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun run ci
- **[unresolved_action]** required_main_agent_actions: adv_run_test could not record red/green/verify phase evidence this session (TemporalQueryTimeout on every attempt; adv_temporal_reconnect succeeded but did not restore the adv_run_test signal path). Verification was captured via direct bash with explicit exit codes instead. Restore the shared Temporal service layer and, if tool-recorded phase evidence is required for acceptance, re-run the three phases with adv_run_test.
- **[archive_only_evidence]** decisions: Bumped src/constants.ts PLUGIN_VERSION 1.10.1->1.10.2 in addition to the four enumerated files and added a consistency test tying it to package.json — PLUGIN_VERSION is the plugin's runtime self-reported version surfaced in tool metadata (index.ts). Leaving it at 1.10.1 would make the running plugin mis-report its release, violating SC4 ('release metadata all identify 1.10.2'). It is the same stale-version class as the README pin, so related-scan/campsite required aligning it. Metadata string only; no behavior change.
- **[archive_only_evidence]** decisions: Left bun.lock untouched — The bump is version-only with no dependency change. bun.lock workspaces[''] records the root package name and dependency ranges but not the root package version, so a version-only bump does not require lock regeneration.
- **[archive_only_evidence]** decisions: CHANGELOG [1.10.2] notes describe the recovery policy and cross-surface regression tests — That is the actual content of this change (README/index.ts/instructions recovery-policy docs + tests, diff since 1.10.1). The tag-release workflow feeds this body to `gh release create --notes-file`, so it must be non-empty and recovery-policy accurate.
- **[archive_only_evidence]** verification: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section" (1) — RED (expected): 4 fail / 0 pass before metadata bump — README still had #v1.10.1, package.json version was 1.10.1, CHANGELOG had no 1.10.2 section. Captured via bash because adv_run_test returned TemporalQueryTimeout.
- **[archive_only_evidence]** verification: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section|PLUGIN_VERSION matches" (0) — GREEN: 5 pass / 0 fail after metadata bump (version pin, package.json version, README pin, CHANGELOG non-empty section, PLUGIN_VERSION consistency). Captured via bash (adv_run_test unavailable).
- **[archive_only_evidence]** verification: bun run ci (0) — VERIFY: `bun test && tsc --noEmit` — 137 pass / 0 fail, 295 expect() calls; tsc clean. (Fixed an initial TS2769 in the new PLUGIN_VERSION test by narrowing `string | undefined` before toBe; final run green.) Captured via bash (adv_run_test unavailable).
- **[archive_only_evidence]** verification: awk '/^## \[1.10.2\]/{flag=1; next} /^## \[/{flag=0} flag' CHANGELOG.md (0) — Release-workflow extraction simulation: yields a non-empty notes body (the two recovery-policy bullets), confirming `gh release create --notes-file` will receive real notes for v1.10.2.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section"
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section|PLUGIN_VERSION matches"
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun run ci
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: awk '/^## \[1.10.2\]/{flag=1; next} /^## \[/{flag=0} flag' CHANGELOG.md
- **[wisdom_candidate]** wisdom_candidates: [success] Release-metadata regression tests should assert package, README pin, runtime version constant, and non-empty changelog section together; this catches partial patch-version bumps before tag-triggered release notes are generated.
- **[archive_only_evidence]** verification: tests_run=bun test -t "release metadata consistency", bun test -t "native edit recovery policy" results=pass — Focused metadata tests: 4 pass, 0 fail, 10 assertions. Focused recovery-policy tests: 3 pass, 0 fail, 38 assertions. Inspected final diff against origin/trunk (a5ebcc8): only CHANGELOG.md, README.md, index.test.ts, index.ts, instructions/morph-tools.md, package.json, and src/constants.ts changed; git diff --check passed; working tree clean. Per packet instruction, full CI was not rerun; supplied final CI evidence is 137 pass / 0 fail.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun test -t "release metadata consistency"
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bun test -t "native edit recovery policy"
- **[agenda]** follow_ups: Planning: write drift tests asserting the recovery rule in index.ts morph_edit description string AND instructions/morph-tools.md AND README.md (extends existing index.test.ts readFileSync pattern at L29-80) to satisfy SC4/AC5/DONT4.
- **[agenda]** follow_ups: Planning: verify the new wording does not weaken the existing Morph-API-failure/timeout -> native edit FALLBACK line in index.ts (L273) per AC4/DONT3.
- **[archive_only_evidence]** sources: OpenCode official plugins docs (tool.execute.before intercepts args; throw to block): tool.execute.before receives output.args and can mutate args or throw to block a tool BEFORE it runs (e.g. reject .env reads, escape bash commands). No documented contract to observe a native tool's failure and substitute a retry or reroute.
- **[archive_only_evidence]** sources: OpenCode-Book 13.2 Plugin Lifecycle Hooks — exact hook signatures: tool.execute.before(input,{args})=>Promise<void> modifies input args only; tool.execute.after(input,{title,output,metadata})=>Promise<void> modifies display output only. Both return void. No failure signal, no pass/fail branch, no re-invocation path. Confirms plugin cannot intercept/retry a native tool failure.
- **[archive_only_evidence]** sources: OpenCode issue #31680 — before-hook arg mutation was a no-op: Upstream bug: tool.execute.before arg mutation historically had no effect because the executor read original args, not hook output. Demonstrates the interception surface is fragile and unsuitable as a reliable native-edit retry/reroute substrate.
- **[archive_only_evidence]** sources: Local: index.ts morph_edit tool description + tool.execute.after hook: Embedded morph_edit description already carries WHEN-TO-USE routing and a Morph-API-failure/timeout -> native edit FALLBACK line (L273). The only execution hook used is tool.execute.after for TUI title branding (L330) — display only, consistent with docs.
- **[archive_only_evidence]** sources: Local: instructions/morph-tools.md always-on routing policy: Canonical always-on instruction with First-Action table and Fallback Policy. Currently covers initial selection and Morph->native fallback only; has no post-native-edit-failure recovery branch — the exact gap the design fills.
- **[archive_only_evidence]** sources: Local: index.test.ts drift tests read all owned surfaces: Tests already readFileSync both instructions/morph-tools.md and README.md and assert prose content; the pattern extends cleanly to assert the recovery rule across all three surfaces (index.ts description, instruction, README), satisfying SC4/AC5 and DONT4.
- **[archive_only_evidence]** sources: Local: prior archived change captured the same ownership boundary: Prior research already recorded that plugin docs establish tool.execute.before argument interception but NOT a native-tool failure rewrite/retry contract — corroborating the boundary independently across two research passes.
- **[archive_only_evidence]** architecture_assessment: The design adds a single, ordered, prose-only native-edit recovery policy (re-read -> at most one corrected native edit only if still small/exact -> otherwise morph_edit) to three plugin-owned routing surfaces (index.ts tool description, instructions/morph-tools.md, README.md), preserves the existing Morph-API-failure/timeout -> native-edit fallback, and locks all three surfaces with drift tests. I independently verified the OpenCode plugin hook contract against two authoritative docs and one upstream issue. The hook surface is decisive: tool.execute.before can only mutate/throw on ARGS before a tool runs and tool.execute.after can only rewrite DISPLAY output after it runs; both return Promise<void> with no failure signal and no re-invocation capability. Therefore a plugin CANNOT observe a native edit's exact-match failure and automatically retry it or reroute to morph_edit. Automatic interception/retry is not supported by the platform. Prose-only guidance targeting the agent's own next action is the correct, and only, supported mechanism — this is the boring, proven solution (documentation + regression tests) over a novel unsupported runtime interception loop. Scope is correctly bounded: it does not touch native edit, the Morph API contract, safety guards, path confinement, secret scrubbing, readonly protections, or adv-engineer config. The design is internally consistent with the approved contract (SC1-SC5, AC1-AC5, C1-C4, DONT1-DONT4). Minor non-blocking execution risk: the plugin CANNOT structurally enforce 'at most one corrected native edit' (that bound is agent-behavioral, not machine-checkable), and tests can only assert prose PRESENCE/consistency, not runtime adherence — but this is inherent to any prose-routing change and the design already discloses it via C1 (must not claim automatic interception).
- **[wisdom_candidate]** wisdom_candidates: [pattern] Content-policy regression tests should assert recovery-specific phrases and boundaries per owned surface; generic tokens such as tool names or a broad write mention can create false confidence after policy drift.
- **[archive_only_evidence]** changes_made: index.test.ts: Strengthened all three policy-surface assertions: require native mismatch trigger, full bounded recovery sequence, explicit write-only boundary, and retained native-edit API fallback. Replaced weak generic anchors that could pass after omission of recovery-specific policy.
- **[archive_only_evidence]** verification: tests_run=bun test -t "native edit recovery policy" results=pass — Focused post-remediation verification passed: 3/3 tests, 38 assertions, 0 failures. git diff --check from trunk merge-base to HEAD was clean. Checkpoint evidence recorded bun run ci passing at 055f29e; not duplicated because review change only strengthened focused test assertions.
- **[archive_only_evidence]** findings: [info] Final reviewed HEAD and CI suite pass.
- **[unresolved_action]** suggested_handoff: Final reviewed policy/test change is verified and ready for acceptance review. — in_scope: Verify final branch HEAD and complete local CI after reviewer test-strengthening commits.
- **[unresolved_action]** required_main_agent_actions: Restore docs/checklists/harden-checklist.md to the change worktree or provide its authoritative content.
- **[unresolved_action]** required_main_agent_actions: After the checklist is available, rerun this hardening review against its explicit controls before release. No code or documentation remediation was required from the reviewed final delta.
- **[unresolved_action]** required_main_agent_actions: Do not revisit final HEAD 631d995 implementation, recovery-policy wording, packaging list, or focused test suite unless the restored checklist identifies a concrete discrepancy.
- **[wisdom_candidate]** wisdom_candidates: [pattern] For instruction-only recovery policies, keep the embedded tool description, packaged instruction, and README aligned and test the same canonical anchors across all three surfaces.
- **[archive_only_evidence]** verification: tests_run=test -e "docs/checklists/harden-checklist.md" && echo OK || echo MISSING, git status --short && git rev-parse HEAD && git log --oneline -5 && git show --stat --oneline --summary 631d995, bun test index.test.ts, git diff --check a5ebcc8..631d995, git diff a5ebcc8..631d995 -- index.ts index.test.ts README.md instructions/morph-tools.md results=pass — Final HEAD is 631d995e5952794c14992c35b9919a00999ba850 and worktree is clean. Focused suite passed: 133 tests, 0 failures, 285 assertions. Diff check produced no whitespace errors. Reviewed final delta: bounded native-edit recovery policy is consistently present in index.ts, instructions/morph-tools.md, and README.md; index.test.ts locks those three surfaces together and confirms Morph API error/timeout -> native edit fallback remains. Test/TDD evidence: regression coverage exists and passes. Slop: no redundant runtime layer or generated/dead artifact added; tests are concise policy-drift assertions. Documentation hygiene: README Unreleased notes and packaged instructions match embedded description. Cleanup: clean worktree; four intentional source/docs/test files only; no generated artifacts. Production readiness: recovery directs reread, forbids unchanged retry, bounds corrected native edit to one exact retry, then routes broader repairs to morph_edit; original API fallback retained. Deployment readiness: package manifest includes index.ts, src/, README.md, LICENSE, instructions/, so changed packaged instruction and README are included. However, docs/checklists/harden-checklist.md is missing, so checklist-specific release attestation is blocked.
- **[wisdom_candidate]** wisdom_candidates: [pattern] Bounded instruction-only behavior benefits from one canonical anchor list tested against each owned delivery surface, preventing documentation drift without adding runtime machinery.
- **[archive_only_evidence]** verification: tests_run=git status --short && git rev-parse HEAD && git diff --check a5ebcc8..631d995 && git diff --stat a5ebcc8..631d995 && git diff a5ebcc8..631d995 -- README.md instructions/morph-tools.md index.ts index.test.ts, bun test index.test.ts (final verifier evidence on unchanged HEAD), tsc --noEmit (final verifier evidence on unchanged HEAD) results=pass — PASS 1 Test/TDD: final verifier recorded bun test index.test.ts: 133 pass, 0 fail, and tsc --noEmit clean on unchanged HEAD 631d995; new regression tests assert all canonical recovery anchors plus preserved API-error/timeout fallback across index.ts, packaged instruction, and README. PASS 2 AI slop/type erosion: 100-line delta is declarative policy/docs plus focused content-level drift tests; no placeholders, casts, any types, broad catches, naive retry loop, or redundant runtime abstraction added. PASS 3 Documentation hygiene: the same bounded five-step rule appears in the embedded tool description, packaged instruction, README troubleshooting section, and Unreleased changelog; wording differentiates native-edit recovery from existing Morph API fallback, with no conflict or stale release claim. PASS 4 Cleanup: final diff contains only four intentional tracked files; worktree is clean; no debug output, temp artifact, dead import, orphaned test, or whitespace error found. PASS 5 Production readiness: no runtime/safety/API/dependency code changed. Policy explicitly requires reread before retry, prohibits unchanged retry, permits at most one narrowly proven native retry, directs broader repairs to morph_edit, retains intentional full-file write boundary, and preserves existing API-error/timeout -> native-edit fallback. PASS 6 Deployment readiness: N/A for env, migration, external-service, CI/CD, infrastructure, feature flag, and deployment steps because the final delta is documentation/tool-description/test text only. Packaging impact is PASS: package.json already ships index.ts, README.md, and instructions/, covering all changed distributable surfaces. Final HEAD remains 631d995e5952794c14992c35b9919a00999ba850; git diff --check is clean.
- **[agenda]** follow_ups: Confirm ~/.config/opencode/package.json edit will preserve the two unrelated deps (@opencode-ai/plugin pin, opencode-firecrawl) — only the morph line gets #v1.10.2.
- **[agenda]** follow_ups: During local deploy, verify installed version by reading the installed node_modules manifest, not by assuming bun add succeeded, because a cached git ref can leave an older commit in place with no lockfile to enforce the pin.
- **[archive_only_evidence]** sources: Bun install/CI docs (bun ci = frozen lockfile): bun ci is a real command equal to `bun install --frozen-lockfile`; installs exact versions from bun.lock and FAILS the build if package.json is out of sync with bun.lock. Requires bun.lock committed. Confirms release.yml line 22 `bun ci` is valid.
- **[archive_only_evidence]** sources: Bun git-dependency docs (commit-ish pinning): github:user/repo without a commit-ish resolves the latest commit on the default branch; a #v1.10.2 tag suffix pins a specific tag. Confirms deterministic-deploy design requires the #v1.10.2 pin.
- **[archive_only_evidence]** sources: Bun re-resolution / lockfile reset docs: Documents rm -rf node_modules + rm bun.lock + bun install as the reliable re-install path when dependency resolution must be forced. Basis for the git-tag re-resolution hazard note.
- **[archive_only_evidence]** sources: Local project package.json (release repo): version currently 1.10.1; ci script = `bun test && tsc --noEmit`. Version bump to 1.10.2 changes package.json name/version block.
- **[archive_only_evidence]** sources: Local project release.yml: Triggers on tags v*; runs `bun ci` then `bun test`+`typecheck`; awk-extracts `## [VERSION]` notes from CHANGELOG.md (version = ref name minus leading v). Creates/edits gh release idempotently.
- **[archive_only_evidence]** sources: Local project CHANGELOG.md: Has empty `## [Unreleased]` then `## [1.10.1]`. NO `## [1.10.2]` section exists yet. awk extract for v1.10.2 would emit empty notes unless a non-empty section is added first.
- **[archive_only_evidence]** sources: Global OpenCode config package.json: opencode-morph-fast-apply pinned as `github:JRedeker/opencode-morph-fast-apply` (UNPINNED = latest default-branch commit). No lockfile present in ~/.config/opencode (glob *.lock* empty). Local deploy pin to #v1.10.2 needed for determinism.
- **[archive_only_evidence]** sources: Canonical tag-after-merge practice: By-the-book consensus: tag stable code on the default branch, never feature/PR branches (tag drift on rebase/force-push, branch deletion, tag points to non-released commit). Matches design's tag-only-after-merge-proof ordering.
- **[archive_only_evidence]** sources: Tag reachability from default branch (git describe / merge-base): A tag is only reachable from the default branch once its commit is merged there; git merge-base --is-ancestor / git branch --contains are the canonical reachability checks. Supports AC5 default-branch reachability proof gate.
- **[archive_only_evidence]** architecture_assessment: The expanded design is architecturally sound and follows by-the-book release/deployment sequencing. Source-authored release-prep (version + CHANGELOG + test expectations) committed on the change branch, ADV archive merging to the default branch, tag-only-after-merge-proof, tag-triggered GitHub Actions release, then a separate deterministic local-deploy step (pin existing git dependency to #v1.10.2, install, sync stable instruction, backup non-secret config, verify installed version + `opencode debug config`, require restart) is the correct shape. Two integrity gaps and several ordering hazards must be respected during execution: (1) release.yml `bun ci` is frozen-lockfile install and FAILS if package.json is out of sync with the tracked bun.lock — a version bump to 1.10.2 changes package.json, so bun.lock must be regenerated/synced in the SAME release-prep commit or CI will fail on the tag. (2) CHANGELOG.md currently has no `## [1.10.2]` section (only empty Unreleased + 1.10.1); the awk extractor emits empty release notes unless a non-empty 1.10.2 section is authored first (agreement SC5/AC-changelog already requires this). (3) Local deploy pins an UNPINNED github: dependency with no lockfile in ~/.config/opencode; git-tag pins can be served from Bun's cache, so re-resolution should be forced (explicit #v1.10.2 in package.json + install; if the installed version does not read 1.10.2, clear node_modules and reinstall) and verified by reading the installed manifest version, not assumed. (4) Config-integrity ordering: backup non-secret config state BEFORE editing ~/.config/opencode/package.json (constraint C6), and treat stable-instruction sync as mandatory co-equal with the package pin (avoidance A5 — do not update the package while leaving the instruction stale). None of these compromise the accepted policy-only Morph-recovery contract; they are release-mechanics correctness requirements the agreement already largely encodes.
- **[agenda]** follow_ups: Planning: encode a pre-push reachability gate (`git merge-base --is-ancestor <tag-commit> origin/<default>`) in the post-Phase-9 tag step, mirroring flipt-io/flipt acda1ab, to prevent orphan tags structurally.
- **[agenda]** follow_ups: Planning: ensure any bun.lock regeneration triggered by the version bump is committed within the acceptance-scoped source-prep step so the tag-triggered release workflow's frozen-lockfile `bun ci` passes at the tagged commit.
- **[agenda]** follow_ups: Note: the briefing packet (_briefingPacket) was truncated by output budget; validation relied on the authoritative _design and _agreement artifacts plus direct in-worktree workflow/manifest reads, which were sufficient for this design-boundary question.
- **[archive_only_evidence]** sources: llmbestpractices — GitHub Releases (canonical release convention): 'Every release tag points to a commit on main.' Confirms release tags must originate from the default branch, not a feature branch. Directly validates design step 3 (tag only at merged default-branch commit).
- **[archive_only_evidence]** sources: PyPA gh-action-pypi-publish Discussion #42 (release-PR tag ownership): Authoritative description of the exact tension in this design: a tag on a release/feature branch 'does not belong to main and can easily be deleted'; only after the PR merges does the tag 'start belonging to main'. Confirms feature-branch tag publication is the risk being avoided.
- **[archive_only_evidence]** sources: flipt-io/flipt commit acda1ab — tag squashed merge commit and verify reachability (#6083): Real-world release-process fix for the exact orphan-tag failure mode: squash-merge produces a NEW commit SHA; tagging the local release-branch commit orphans the tag (not an ancestor of default branch). Fix = hard-sync to origin default, confirm HEAD is squashed commit, add `git merge-base --is-ancestor` reachability gate before pushing the tag. Mirrors this design's Phase-9-merge-proof-then-tag-then-verify ordering.
- **[archive_only_evidence]** sources: git-scm git-tag / Git Basics Tagging docs: Annotated-vs-lightweight tag semantics; a commit not reachable from any branch is 'unreachable, except by the exact commit hash'. Confirms tag reachability from the default branch is the correct structural proof, and annotated tags (design's choice) carry tagger/date/message metadata.
- **[archive_only_evidence]** sources: DEV — Automating releases with GitHub Actions (anti-pattern statement): 'Do not release production directly from a random feature branch. That is not CI/CD. That is gambling.' Production release triggers from tags on the release-ready branch, not feature merges. Reinforces the boundary.
- **[archive_only_evidence]** sources: In-repo release workflow .github/workflows/release.yml (verified in worktree): Confirmed: workflow triggers on `push: tags: ['v*']`, runs `bun ci` + `bun test` + typecheck, extracts notes via awk from `## [VERSION]` CHANGELOG section, and `gh release create`. Validates every downstream dependency claim in the design: tag-triggered, non-empty changelog section required, GitHub release created from tag.
- **[archive_only_evidence]** sources: In-repo package.json + ci.yml (verified in worktree): Current version 1.10.1 (design targets 1.10.2 patch — correct semver for docs/coverage correction with no API break). `ci` script = `bun test && tsc --noEmit`; CI runs on trunk push/PR. `bun ci` install is frozen-lockfile, so package/lock skew fails, validating the design's lock-consistency failure-handling row.
- **[archive_only_evidence]** architecture_assessment: The corrected design boundary is architecturally sound and matches canonical release-engineering practice. The core split — (1) source release preparation (package.json version, README pinned example, index.test.ts version/changelog assertions, non-empty CHANGELOG `## [1.10.2]` section, frozen-lockfile-clean bun.lock) is acceptance-scoped and completable pre-merge; (2) annotated tag creation, tag-triggered GitHub release publication/verification, and local deployment are mandatory post-Phase-9 operations gated on default-branch merge proof — is exactly the ordering that authoritative sources prescribe. The design correctly recognizes that tag/release CANNOT be truthful acceptance criteria for pre-merge work, because a tag pushed before merge would publish an unmerged feature-branch commit (llmbestpractices: tags point to commits on main; PyPA #42: a pre-merge tag 'does not belong to main'). The flipt-io/flipt fix (acda1ab) is a production precedent for this exact orphan-tag hazard under squash-merge and validates the design's step 5 reachability verification (tag target must equal the merged default-branch commit) as the correct structural gate rather than a heuristic. The design's failure-handling table ('Phase 9 merge unavailable → do not tag, publish, or deploy') and its rejected alternative ('Tag before merge: can publish unmerged code') are both correct and directly supported. The deployment half (pin global dependency to `github:...#v1.10.2`, verify installed manifest == 1.10.2, sync stable instruction path, report restart requirement) is deterministic and matches the design's own prior researcher-confirmed pinning rationale; unpinned default-branch resolution cannot prove deployed version, so pinning is the correct choice. Boundary preserves the user-requested end state: source metadata shipped and verifiable pre-merge, and tag/GitHub-release/local-deploy all completed after merge proof — nothing dropped, only re-sequenced to be truthful.

## Contract / AC Coverage

| ID | Kind | Status |
| --- | --- | --- |
| SC1 | success_criterion | pass |
| SC2 | success_criterion | pass |
| SC3 | success_criterion | pass |
| SC4 | success_criterion | pass |
| SC5 | success_criterion | pass |
| AC1 | acceptance_criterion | pass |
| AC2 | acceptance_criterion | pass |
| AC3 | acceptance_criterion | pass |
| AC4 | acceptance_criterion | pass |
| C1 | constraint | respected |
| C2 | constraint | respected |
| C3 | constraint | respected |
| C4 | constraint | respected |
| C5 | constraint | respected |
| C6 | constraint | respected |
| DONT1 | avoidance | respected |
| DONT2 | avoidance | respected |
| DONT3 | avoidance | respected |
| DONT4 | avoidance | respected |
| DONT5 | avoidance | respected |

## Unresolved Actions

- verification_missing: No adv_run_test evidence found for reported command: bun /tmp/opencode/morph_recovery_red_check.mjs <worktree>
- verification_missing: No adv_run_test evidence found for reported command: bun test -t "native edit recovery policy"
- verification_missing: No adv_run_test evidence found for reported command: bun run ci
- adv_run_test could not record red/green/verify phase evidence this session (TemporalQueryTimeout on every attempt; adv_temporal_reconnect succeeded but did not restore the adv_run_test signal path). Verification was captured via direct bash with explicit exit codes instead. Restore the shared Temporal service layer and, if tool-recorded phase evidence is required for acceptance, re-run the three phases with adv_run_test.
- verification_missing: No adv_run_test evidence found for reported command: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section"
- verification_missing: No adv_run_test evidence found for reported command: bun test index.test.ts -t "version pin|release version|pinned-version example|non-empty section|PLUGIN_VERSION matches"
- verification_missing: No adv_run_test evidence found for reported command: bun run ci
- verification_missing: No adv_run_test evidence found for reported command: awk '/^## \[1.10.2\]/{flag=1; next} /^## \[/{flag=0} flag' CHANGELOG.md
- verification_missing: No adv_run_test evidence found for reported command: bun test -t "release metadata consistency"
- verification_missing: No adv_run_test evidence found for reported command: bun test -t "native edit recovery policy"
- Final reviewed policy/test change is verified and ready for acceptance review. — in_scope: Verify final branch HEAD and complete local CI after reviewer test-strengthening commits.
- Restore docs/checklists/harden-checklist.md to the change worktree or provide its authoritative content.
- After the checklist is available, rerun this hardening review against its explicit controls before release. No code or documentation remediation was required from the reviewed final delta.
- Do not revisit final HEAD 631d995 implementation, recovery-policy wording, packaging list, or focused test suite unless the restored checklist identifies a concrete discrepancy.
